#ifndef LEVELLIST_HPP
#define LEVELLIST_HPP

#include <map>
#include <vector>
#include <Box2D/Box2D.h>
#include "../Entity/AbstractEntity.hpp"
/*
typedef std::map<AbstractEntity*, b2Transform> LevelEntities;
typedef std::vector<Level> LevelVector;

struct Level {

	LevelEntities entities;
	bool endLevel(b2Body* const playerBody);
}

class LevelList {

	public:
	
};
 TODO redo this*/
#endif